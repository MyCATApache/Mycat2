sql:
  - sql: /*!40101 set @@session.wait_timeout=28800 */
    code: return responseOk();
  - sql: select DB2.TABLE2.id from DB1.TABLE,DB2.TABLE2
    code: return Integer.valueOf(0);
  - sql: select 1;
    code: return Integer.valueOf(1);
schema:
  - table: DB1.TABLE,DB2.TABLE2
    defaultCode: return Integer.valueOf(3);
    sql:
      - sql: select * from {tables}
        code: return Integer.valueOf(2);
lib:
  - io.mycat.lib.BaseLib
